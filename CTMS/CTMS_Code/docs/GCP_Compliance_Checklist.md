# GCP 合规检查清单

| ICH-GCP 参考条款 | 要求 | CTMS 实现 | 状态 |
|-------------------|-------------|---------------------|--------|
| **1.5** | 记录应准确、完整、清晰且及时。 | 所有模型的审计追踪 (`simple_history`) 记录变更。 | ✅ 已实现 |
| **3.1.2** | 启动前需获得 IRB/IEC 批准。 | `Trial.status` 工作流强制在“进行中”状态前获得 IRB 批准。已添加 IRB 字段。 | ✅ 已实现 |
| **4.1.1** | 研究者资格。 | `User.role` = 'INV'，简历存储在 `Document` 模块中。 | ✅ 已实现 |
| **4.8.10** | 知情同意。 | 追踪 `Subject.informed_consent_date`。入组时进行验证。 | ✅ 已实现 |
| **4.11.1** | SAE 报告（24小时内）。 | `AdverseEvent.is_serious` 触发警报逻辑。带时间戳的 `reported_to_pv_at`。 | ✅ 已实现 |
| **5.5.1** | 申办方数据处理（验证）。 | 序列化器中的数据验证规则（例如日期逻辑）。在线文档编辑通过悲观锁防止并发冲突。 | ✅ 已实现 |
| **5.18.3** | 监查（SDV）。 | CRA 将 `Visit.data_status` 设为 'VERIFIED'（SDV 完成）。新的 SDV API 端点。审计追踪覆盖 `MonitoringVisit`。 | ✅ 已实现 |
| **5.18.6** | 方案违背报告。 | `ProtocolDeviation` 模型追踪违背情况。审计追踪覆盖 `ProtocolDeviation`。 | ✅ 已实现 |
| **8.1** | 必备文件。 | `Trial` 和 `Site` 模型存储方案/中心信息。支持在线文档创建与版本控制。 | ✅ 已实现 |
| **8.3.20** | 受试者筛选日志。 | `Subject` 模型追踪筛选/筛选失败。 | ✅ 已实现 |

## 21 CFR Part 11 合规性

| 子部分 | 要求 | 实现 | 状态 |
|---------|-------------|----------------|--------|
| **11.10(b)** | 审计追踪 | `django-simple-history` 记录每次变更的 `history_date`、`history_user`、`history_type`。 | ✅ 已实现 |
| **11.10(d)** | 系统访问 | 具有密码的唯一 `User` 账户（Django 认证）。 | ✅ 已实现 |
| **11.10(e)** | 审计追踪完整性 | 历史表是独立的且仅追加（通过 Django ORM）。 | ✅ 已实现 |
| **11.50(a)** | 电子签名 | 签署访视时的密码验证。在 `SignatureLog` 中追踪原因/日期/用户/IP。 | ✅ 已实现 |
| **11.70** | 签名/记录链接 | 签名与记录（访视状态更新）加密链接。 | ✅ 已实现 |
| **11.200(a)** | 签名组件 | 签名至少包含两个不同的组件（用户 ID 和密码）。 | ✅ 已实现 |
