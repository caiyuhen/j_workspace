================================================================================
Wave 8.4 T6 ReportGeneratorPanel  任务摘要 (TDD 15/15 GREEN)
================================================================================

【日期】2026-08-18
【工作目录】d:\workspace\MedA

================================================================================
1) 组件核心: 常量 + 行数
================================================================================

【文件路径】
  - 组件: packages/shared-ui/src/components/ReportGeneratorPanel.tsx  (371 行)
  - 测试: packages/shared-ui/src/__tests__/ReportGeneratorPanel.test.tsx (179 行)

【导出常量】HTTP_422_DETAIL_MAP (5 个 key literal 完全匹配 Spec §3):
  ├─> grade_less_than_one
  │   = "GRADE 评估少于 1 项，请先至少完成一个结局指标的 GRADE 评估（推荐：关键结局必评 High/Moderate/Low/VeryLow）"
  ├─> prisma_less_than_five
  │   = "PRISMA 2020 勾选少于 5 项，请勾选 Protocol 部分的 5 项强制条目（第 1/2/3a/4/5 项）"
  ├─> forest_no_svg_content
  │   = "Meta 分析生成失败：森林图 SVG 为空，请先确认筛选阶段 ≥ 2 个研究、且二分类结局 RR/OR 设置正确"
  ├─> sof_no_grade_rows
  │   = "SoF 12 列表无任何 GRADE 行数据，请在 Stage 5 GRADE Tab 保存后再返回 Output 生成报告"
  └─> no_evidences_selected
      = "证据抽取后 0 条符合条件，返回 Stage 4 Data Extraction 确认数据抽取标记 = include"

【组件 Props 类型 ReportGeneratorPanelProps】
  - projectId: number
  - sha256?: string | null         → 显示为 sha256_ 前缀 + 12 位前缀 + ... + 4 位后缀
  - versionLabel?: string | null   → 版本徽章 (紫色 badge)
  - errorDetailLiteral?: string | null → 422 错误 detail key
  - activeTab: "editor" | "md" | "html"
  - onActiveTabChange: (next) => void
  - editorSlot: React.ReactNode    → 外部注入 Editor
  - mdPreviewSlot: string | null   → Markdown 原文
  - htmlPreviewSlot?: string | null → HTML 原文 (传入 HtmlPreviewFrame)
  - onGenerateClick: () => void
  - onExportClick?: () => void
  - isGenerating?: boolean (生成 loading)
  - isExporting?: boolean
  - generatedAt?: string | null   → time ago 展示
  - exportButtons?: Array<{label; onClick; testId?; variant?}>

【渲染结构】
  顶部 header:
    ├─ 标题: "报告生成器" (h2)
    └─ SHA 行 (当 sha256 存在时渲染):
        ├─ sha-value (monospace, 12前缀...4后缀)
        ├─ version-label 徽章
        └─ generated-at time-ago
  错误条: data-testid=err-detail (HTTP_422_DETAIL_MAP key 命中 → 中文; 否则 → 原文)
  Tab 栏: role=tablist
    ├─ tab-editor / tab-md / tab-html  (aria-selected 高亮)
    └─ 点击触发 onActiveTabChange(tab)
  Tab Body:
    ├─ editor: tab-body-editor > editorSlot
    ├─ md:     <pre data-testid=md-previewer> mdPreviewSlot </pre>
    └─ html:   tab-body-html > HtmlPreviewFrame (iframe sandbox)
  底部 actions row:
    ├─ btn-generate-report (primary, disabled+spinner 当 isGenerating)
    ├─ btn-export-report   (secondary, 仅当 onExportClick 存在)
    └─ exportButtons[] 追加 (variant 支持 primary / ghost)

【0 新增第三方依赖】
  - 复用: React, HtmlPreviewFrame (内部组件相对路径 import)
  - 样式: 纯 inline React.CSSProperties (无 tailwind/clsx 引入)

================================================================================
2) 15/15 GREEN 证据
================================================================================

运行命令:
  npx vitest run ReportGeneratorPanel --reporter=verbose --run
  (在 packages/shared-ui/ 目录下)

Vitest v2.1.9 结果:
  Test Files  1 passed (1)
  Tests       15 passed (15)
  Duration    17.31s

测试矩阵 (P1~P15):
  ✅ P1 渲染三 Tab (tab-editor/tab-md/tab-html) 全部存在
  ✅ P2 activeTab='editor' → tab-editor aria-selected=true (高亮)
  ✅ P3 点击 tab-md → 回调 onActiveTabChange("md") 触发
  ✅ P4 errorDetailLiteral=prisma_less_than_five → err-detail 包含「PRISMA 2020」中文
  ✅ P5 errorDetailLiteral=forest_no_svg_content → 包含「森林图 SVG 为空」
  ✅ P6 errorDetailLiteral=unknown_key_not_in_map → 原文展示回退
  ✅ P7 sha256=null → 不渲染 sha-row
  ✅ P8 sha256=sha256_aabbccddeeff0011223344556677889900 → 前缀 12 + ... + 后缀 4
  ✅ P9 versionLabel=v0.1-draft → version-label 渲染
  ✅ P10 editorSlot → t-inject 子节点正确显示
  ✅ P11 mdPreviewSlot 字符串 → md-previewer <pre> 文本内容匹配
  ✅ P12 isGenerating=true → btn disabled + btn-spinner 存在
  ✅ P13 btn-generate-report click → onGenerateClick 触发
  ✅ P14 btn-export-report click → onExportClick 触发
  ✅ P15 exportButtons 2 项 (下载MD/下载HTML) → 各自 onClick 独立触发

详细日志: d:\workspace\MedA\T6_GREEN.txt

================================================================================
3) Hard-Gate 核查
================================================================================

  ✓ 0 新增第三方依赖 (package.json 未变更)
  ✓ 5 个 HTTP_422_DETAIL_MAP key literal 完全匹配 (逐字精确):
      [grade_less_than_one, prisma_less_than_five, forest_no_svg_content,
       sof_no_grade_rows, no_evidences_selected]
  ✓ data-testid 严格按 Spec:
      tab-editor / tab-md / tab-html / err-detail / md-previewer /
      btn-generate-report / btn-export-report
  ✓ 未修改 shared-ui/index.ts (T9 统一 barrel)
  ✓ SHA 不变量: sha256_ 前缀保留，12 位前缀 + ... + 4 位后缀格式

================================================================================
