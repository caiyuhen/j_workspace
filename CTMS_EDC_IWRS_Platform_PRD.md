# CTMS+EDC+IWRS 一体化 SaaS 平台产品规格文档（PRD）

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| **产品名称** | CTMS+EDC+IWRS 一体化临床试验管理平台 |
| **版本** | v1.0 |
| **文档状态** | 草案 |
| **撰写日期** | 2026 年 5 月 27 日 |
| **目标架构** | 微服务 + SaaS |
| **开发模式** | AI 驱动开发 + AI 自动化测试 |

---

## 1. 问题陈述（Problem Statement）

### 1.1 核心问题

当前临床试验行业存在以下痛点：

1. **系统割裂**：CTMS（临床试验管理系统）、EDC（电子数据采集）、IWRS（随机化与药物供应管理）通常由不同供应商提供，数据孤岛严重，需要大量手工对账和整合工作。

2. **合规成本高**：临床试验数据需要符合 CDISC（SDTM/ADaM）、CDASH、GCP 等多重标准，现有系统往往需要复杂的配置和定制开发才能达到合规要求。

3. **医生工具缺失**：研究者个人缺乏灵活的患者管理工具，院外随访数据难以系统化采集，无法有效整合到临床试验中。

4. **文档管理分散**：TMF（试验主文件）管理独立于业务系统，eTMF 功能往往流于形式，无法实现真正的在线协作和实时更新。

5. **SaaS 化不足**：传统临床试验软件部署成本高、升级困难，中小 CRO 和研究机构难以负担。

### 1.2 影响范围

- **目标用户**：制药企业、CRO 公司、临床试验机构、独立研究者
- **问题频率**：每个试验项目全生命周期（通常 1-3 年）持续存在
- **不解决的代价**：
  - 数据整合成本增加 30-50%
  - 合规审核周期延长 20-40%
  - 试验执行效率降低，影响新药上市时间

---

## 2. 目标（Goals）

### 2.1 用户目标

| 目标 | 衡量方式 |
|------|----------|
| 实现 CTMS/EDC/IWRS 数据无缝打通 | 跨系统数据查询响应时间 < 2 秒 |
| 降低 eCRF 设计门槛 | 非技术人员可在 30 分钟内完成基础表单设计 |
| 提升研究者院外随访效率 | 随访数据录入时间减少 50% |
| 简化合规准备 | SDTM 映射自动化覆盖率 > 80% |
| 实现文档在线协作 | eTMF 文档编辑延迟 < 3 秒 |

### 2.2 商业目标

| 目标 | 衡量方式 |
|------|----------|
| 支持多租户 SaaS 模式 | 首年签约 10+ 付费客户 |
| 降低部署成本 | 新客户上线时间 < 2 周 |
| 提升客户粘性 | 年度续约率 > 85% |
| 扩大市场覆盖 | 覆盖中小 CRO 和独立研究者市场 |

---

## 3. 非目标（Non-Goals）

### v1.0 明确不做的事项

| 非目标 | 原因 |
|--------|------|
| **生物统计与分析模块（如 SAS/ADaM 生成）** | 专业性强，需独立产品，v1 聚焦数据收集与管理 |
| **RCT 之外的复杂试验设计（如适应性设计）** | 优先满足 80% 常见场景，复杂设计放入 v2 |
| **移动端原生 App** | 优先响应式 Web，移动端需求验证后决策 |
| **与第三方 CTMS/EDC 的数据对接** | v1 聚焦内部系统整合，集成能力 v2 扩展 |
| **AI 辅助功能（如异常检测、智能质控）** | 基础功能稳定后再叠加 AI 能力 |
| **多语言支持（除中文）** | 首期聚焦中国市场，国际化后续规划 |

---

## 4. 用户故事（User Stories）

### 4.1 CTMS 模块

#### 4.1.1 项目管理

- **US-CTMS-001**：作为项目总监，我想创建和管理多个临床试验项目，以便集中掌控所有试验的进度和状态
- **US-CTMS-002**：作为项目经理，我想查看每个研究中心的进度仪表板，以便及时发现延迟的风险
- **US-CTMS-003**：作为财务，我想追踪每个项目的收支情况，以便控制预算和预测成本
- **US-CTMS-004**：作为运营人员，我想配置项目审批流程，以便确保关键决策经过正确的人员审核

#### 4.1.2 eTMF 功能

- **US-ETMF-001**：作为文档管理员，我想在线上传、编辑、版本化管理 TMF 文档，以便保持文件最新状态
- **US-ETMF-002**：作为监查员，我想快速搜索和下载特定研究中心的文档，以便准备现场访视
- **US-ETMF-003**：作为合规官，我想查看文档的完整审计追踪，以便应对监管检查
- **US-ETMF-004**：作为项目经理，我想收到文档缺失或过期的提醒，以便及时补全 TMF

#### 4.1.3 工时管理

- **US-TIME-001**：作为 CRA/CRC，我想快速填报每日/每周工时，以便记录和核对工作量
- **US-TIME-002**作为项目经理，我想查看团队工时报表，以便评估项目人力投入和成本
- **US-TIME-003**：作为财务，我想导出工时数据进行财务结算，以便准确计算项目成本

### 4.2 EDC 模块

#### 4.2.1 eCRF 设计器

- **US-EDC-001**：作为数据管理员，我想通过拖拉拽方式设计 eCRF 表单，以便快速响应方案变更
- **US-EDC-002**：作为医学经理，我想确保字段名称符合 CDASH 标准，以便数据标准化
- **US-EDC-003**：作为程序员，我想自动将 eCRF 映射到 SDTM 结构，以便减少手工编码工作量
- **US-EDC-004**：作为质量控制员，我想配置字段验证规则（如范围检查、逻辑检查），以便及时发现数据问题
- **US-EDC-005**：作为数据管理员，我想复用已有的表单模板，以便快速创建相似项目的 eCRF

#### 4.2.2 数据采集

- **US-EDC-010**：作为研究者，我想在线录入患者数据，以便减少纸质 CRF 的工作量
- **US-EDC-011**：作为数据管理员，我想查看未查询已解决的数据问题列表，以便跟进数据清理
- **US-EDC-012**：作为监查员，我想导出特定患者的完整数据，以便现场核实
- **US-EDC-013**：作为统计师，我想导出符合 SDTM 标准的数据集，以便直接用于统计分析

### 4.3 IWRS 模块

- **US-IWRS-001**：作为协调员，我想为受试者随机分配治疗方案，以便确保随机化过程公正
- **US-IWRS-002**：作为药物管理员，我想追踪药物库存和供应状态，以避免药物短缺
- **US-IWRS-003**：作为项目经理，我想查看随机化平衡报告，以确保各组分配均衡
- **US-IWRS-004**：作为紧急情况下研究人员，我想破解随机化代码获取治疗方案，以确保患者安全

### 4.4 医生病历夹

- **US-CASE-001**：作为医生，我想为患者创建个人病历档案，以便长期跟踪患者健康状况
- **US-CASE-002**：作为医生，我想自定义数据采集表单，以便适应不同病种的需求
- **US-CASE-003**：作为医生，我想引用 EDC 中的已有模板，以便快速创建标准化采集表
- **US-CASE-004**：作为医生，我想查看患者的历史数据趋势，以便发现病情变化
- **US-CASE-005**：作为医生，我想导出患者数据用于学术研究，以便发表临床研究成果

### 4.5 系统管理

- **US-SYS-001**：作为系统管理员，我想配置多租户隔离策略，以确保客户数据安全
- **US-SYS-002**：作为管理员，我想管理用户角色和权限，以便实现细粒度访问控制
- **US-SYS-003**：作为管理员，我想查看系统审计日志，以便追踪关键操作

---

## 5. 功能需求（Requirements）

### 5.1 系统架构

#### 5.1.1 微服务架构设计（P0）

**需求描述**：
系统采用微服务架构，各业务模块独立部署，通过 API 网关统一暴露接口。

**服务划分**：

```
┌─────────────────────────────────────────────────────────┐
│                     API Gateway                         │
└─────────────────────────────────────────────────────────┘
                          │
    ┌─────────────┬───────┼───────┬─────────────┐
    │             │       │       │             │
┌───▼───┐   ┌────▼────┐ ┌▼────┐ ┌▼──────┐ ┌───▼───┐
│CTMS   │   │  EDC    ││IWRS │ │病历夹 │ │ 共享服务│
│服务   │   │  服务   ││服务 │ │ 服务  │ │(认证/文档)│
└───┬───┘   └────┬────┘ └────┘ └──────┘ └───┬───┘
    │            │                           │
    └────────────┴──────────┬────────────────┘
                            │
                    ┌───────▼────────┐
                    │  统一数据库层  │
                    │  (多租户隔离)  │
                    └────────────────┘
```

**技术栈要求**：
- 后端：Node.js/Python/Java（根据团队技术栈选择）
- 前端：React/Vue + 响应式设计
- 数据库：PostgreSQL（支持 JSONB 灵活 schema）
- 缓存：Redis
- 消息队列：RabbitMQ/Kafka
- 容器化：Docker + Kubernetes

**验收标准**：
- [ ] 各微服务可独立部署和扩展
- [ ] API 响应时间 P95 < 500ms
- [ ] 支持水平扩展，单节点故障不影响整体服务
- [ ] 多租户数据隔离（行级隔离 + 加密）

#### 5.1.2 数据库设计 - CDISC 合规（P0）

**需求描述**：
底层数据库设计需遵循 CDISC 标准，支持 SDTM 域结构。

**核心表结构**：

```sql
-- SDTM 通用结构
-- 每个 SDTM 域对应一张表（如 DM, AE, CM, LB, etc.）

-- 受试者主数据 (DM Domain)
CREATE TABLE sdtm_dm (
    STUDYID VARCHAR(50) NOT NULL,    -- 研究标识
    DOMAIN VARCHAR(2) NOT NULL,      -- 固定为 'DM'
    USUBJID VARCHAR(50) NOT NULL,    -- 唯一受试者 ID
    SUBJID VARCHAR(50),              -- 研究内受试者编号
    RRFSTRTD DATE,                   -- 首次筛选日期
    RFENDDT DATE,                    -- 最后一次接触日期
    DTHFL VARCHAR(1),                -- 死亡标志
    DTHDT DATE,                      -- 死亡日期
    -- ... 更多 SDTM DM 标准字段
    tenant_id VARCHAR(50) NOT NULL,  -- 多租户标识
    audit_trail JSONB,               -- 审计追踪
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 不良事件 (AE Domain)
CREATE TABLE sdtm_ae (
    STUDYID VARCHAR(50) NOT NULL,
    DOMAIN VARCHAR(2) NOT NULL,      -- 固定为 'AE'
    USUBJID VARCHAR(50) NOT NULL,
    AESER VARCHAR(1),                -- 严重不良事件标志
    AEOUT VARCHAR(20),               -- 不良事件结果
    AESTDTC DATETIME,                -- 开始日期时间
    AEENDTC DATETIME,                -- 结束日期时间
    -- ... 更多 SDTM AE 标准字段
    tenant_id VARCHAR(50) NOT NULL,
    audit_trail JSONB,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 更多 SDTM 域表：CM (合并用药), LB (实验室), VS (生命体征), etc.
```

**CDASH 字段元数据**：

```sql
-- CDASH 标准字段定义库
CREATE TABLE cdash_variable_library (
    variable_name VARCHAR(100) PRIMARY KEY,  -- 英文变量名（CDASH 标准）
    variable_label VARCHAR(200),             -- 中文标签
    variable_type VARCHAR(20),               -- 数据类型
    question_text VARCHAR(500),              -- 标准问法
    unit VARCHAR(50),                        -- 单位
    example VARCHAR(100),                    -- 示例值
    category VARCHAR(50),                    -- 分类（人口学、安全、疗效等）
    version VARCHAR(20),                     -- CDASH 版本
    is_required BOOLEAN
);

-- 预填充 CDASH 标准字段（需导入完整 CDASH 变量库）
-- 参考：https://www.cdisc.org/standards/foundational/cdash
```

**eCRF 模板与字段映射**：

```sql
-- EDC 模板定义
CREATE TABLE edc_templates (
    id SERIAL PRIMARY KEY,
    template_name VARCHAR(200) NOT NULL,
    study_id VARCHAR(50),                  -- 关联试验
    owner_type VARCHAR(20),                -- 'EDC' or 'DOCTOR_CASE'
    owner_id VARCHAR(50),                  -- 所有者 ID
    version VARCHAR(20) NOT NULL,
    is_active BOOLEAN DEFAULT true,
    tenant_id VARCHAR(50) NOT NULL,
    created_by VARCHAR(100),
    created_at TIMESTAMP DEFAULT NOW()
);

-- eCRF 表单定义
CREATE TABLE edc_forms (
    id SERIAL PRIMARY KEY,
    template_id INTEGER REFERENCES edc_templates(id),
    form_name VARCHAR(200) NOT NULL,       -- 表单名称
    form_key VARCHAR(100) NOT NULL,        -- 表单键名
    section_order INTEGER,                 -- 排序
    sdtm_domain VARCHAR(20),               -- 映射的 SDTM 域
    sdtm_mapping JSONB,                    -- 字段到 SDTM 的映射规则
    tenant_id VARCHAR(50) NOT NULL
);

-- eCRF 字段定义
CREATE TABLE edc_fields (
    id SERIAL PRIMARY KEY,
    form_id INTEGER REFERENCES edc_forms(id),
    field_name VARCHAR(100) NOT NULL,      -- 英文变量名（CDASH 标准）
    field_label VARCHAR(200) NOT NULL,     -- 中文标签
    field_type VARCHAR(50) NOT NULL,       -- 字段类型（见下方枚举）
    field_order INTEGER,
    required BOOLEAN DEFAULT false,
    validation_rules JSONB,                -- 验证规则（范围、正则、逻辑检查）
    query_config JSONB,                    -- 查询配置
    cdash_variable VARCHAR(100),           -- 关联 CDASH 标准变量
    sdtm_variable VARCHAR(100),            -- 映射的 SDTM 变量
    sdtm_qualifier VARCHAR(50),            -- SDTM 限定符
    tenant_id VARCHAR(50) NOT NULL
);

-- 字段类型枚举：
-- TEXT, NUMBER, DATE, DATETIME, DECIMAL, 
-- RADIO, CHECKBOX, SELECT, MULTISELECT,
-- CALCULATED, SIGNED, IMAGE, FILE,
-- SECTION_HEADER
```

**验收标准**：
- [ ] 所有 SDTM 标准域表已创建
- [ ] CDASH 变量库导入完整（至少覆盖常用变量）
- [ ] eCRF 字段可配置 SDTM 映射
- [ ] 支持 SDTM 数据集导出（CSV/SDTM 数据集格式）
- [ ] 多租户数据隔离（tenant_id 强制校验）

### 5.2 CTMS 模块

#### 5.2.1 项目管理（P0）

**需求描述**：
支持创建、管理临床试验项目全生命周期。

**功能点**：

1. **项目创建与编辑**
   - 项目基本信息：项目编号、名称、方案版本号、治疗领域
   - 项目团队：研究者、CRA、CRC、项目经理分配
   - 时间计划：关键里程碑设置
   - 预算设置：项目总预算、各中心预算

2. **研究中心管理**
   - 中心列表：中心编号、名称、PI 信息、状态
   - 入组进度：目标入组数、实际入组数、入组率
   - 状态跟踪：筛选中、活跃、提前终止、已完成

3. **项目仪表板**
   - 全局视图：所有项目进度概览
   - 单个项目视图：入组趋势、数据质量、问题分布

**验收标准**：
- [ ] 可创建、编辑、归档项目
- [ ] 可管理研究中心列表和状态
- [ ] 仪表板实时显示入组进度
- [ ] 支持项目级别权限控制

#### 5.2.2 审批流程（P0）

**需求描述**：
支持 configurable 的多级审批流程。

**功能点**：

1. **审批节点配置**
   - 支持串行、并行审批
   - 可配置审批人角色（按项目、按中心）
   - 审批意见和附件

2. **审批场景**
   - 方案偏离审批
   - 数据修正审批
   - 药物发运审批
   - 财务报销审批

**验收标准**：
- [ ] 可配置审批流程模板
   - [ ] 审批通知（站内信 + 邮件）
   - [ ] 审批历史记录可追溯

#### 5.2.3 eTMF 电子试验主文件（P0）

**需求描述**：
在线协作式 TMF 文档管理系统。

**功能点**：

1. **TMF 结构**
   - 遵循 RAVEL 模型或 TMF Reference Model
   - 支持自定义文件夹结构
   - 文档分类：必备文件、研究执行文件、报告文件等

2. **文档管理**
   - 在线上传（支持批量）
   - 在线预览（PDF、Word、Excel、图片）
   - 版本控制（自动版本记录）
   - 文档模板
   - 在线标注和评论

3. **文档完整性监控**
   - TMF  checklist 对照
   - 缺失文档提醒
   - 文档过期提醒

4. **审计追踪**
   - 所有文档操作记录（上传、下载、删除、修改）
   - 操作人、时间、IP 地址

**技术实现**：
- 文档存储：对象存储（如 AWS S3、阿里云 OSS）
- 在线预览：集成文档转换服务
- 全文检索：Elasticsearch

**验收标准**：
- [ ] TMF 文件夹结构可配置
- [ ] 支持主流文档格式上传和预览
- [ ] 版本历史可追溯
- [ ] 完整性仪表板实时显示缺失文档
- [ ] 审计追踪完整记录

#### 5.2.4 工时管理（P1）

**需求描述**：
项目工时填报和统计系统。

**功能点**：

1. **工时填报**
   - 日报/周报模式
   - 按项目、按任务分配工时
   - 支持批量填报
   - 工时附件上传

2. **工时审批**
   - 项目经理审批
   - 工时异常提醒

3. **工时报表**
   - 个人工时统计
   - 项目工时汇总
   - 部门/团队工时分析
   - 工时成本分析（结合费率）

**验收标准**：
- [ ] 支持快速填报工时
- [ ] 审批流程可配置
- [ ] 报表支持多维度筛选和导出
- [ ] 工时数据与项目关联

### 5.3 EDC 模块

#### 5.3.1 eCRF 拖拉拽设计器（P0）

**需求描述**：
可视化 eCRF 表单设计器，支持拖拉拽操作。

**功能点**：

1. **设计器界面**
   - 左侧：组件面板（字段类型、CDASH 标准字段库）
   - 中间：画布（表单设计区）
   - 右侧：属性面板（字段配置）

2. **字段配置**
   - 英文名称（自动校验 CDASH 命名规范）
   - 中文标签
   - 字段类型选择
   - 验证规则配置（范围、正则、逻辑检查）
   - 显示/隐藏条件
   - 计算逻辑（如 BMI = 体重/身高²）

3. **CDASH 辅助**
   - CDASH 标准字段推荐（基于表单分类）
   - 命名规范校验（提醒非标准命名）
   - 标准问法引用

4. **SDTM 映射配置**
   - 选择目标 SDTM 域（DM/AE/CM/LB/VS 等）
   - 字段级映射配置
   - 派生变量配置
   - 映射规则保存和复用

5. **模板管理**
   - 保存为模板
   - 模板库浏览
   - 基于模板创建新表单
   - 模板版本管理

**交互原型**：
```
┌──────────────────────────────────────────────────────────┐
│  eCRF 设计器 - [人口学表单]                             │
├──────────┬─────────────────────────────┬─────────────────┤
│          │                             │                 │
│  组件库  │         画布                │   属性面板      │
│          │                             │                 │
│ ┌──────┐ │  ┌──────────────────────┐  │  字段名称 *     │
│ │ TEXT │ │  │ 人口学信息          │  │  [AGE          ]│
│ └──────┘ │  │                      │  │  年龄          │
│ ┌──────┐ │  │  ┌────────────────┐  │  │                 │
│ │NUMBER│ │  │  │ 姓名：________ │  │  │  字段类型       │
│ └──────┘ │  │  │                │  │  │  [文本        ▼]│
│ ┌──────┐ │  │  │  年龄：_______ │  │  │                 │
│ │ DATE │ │  │  │  ▲              │  │  │  CDASH 变量    │
│ └──────┘ │  │  │  │拖拽字段到这里│  │  │  [AGE        ▼]│
│          │  │  │  └────────────────┘  │  │                 │
│ ┌──────┐ │  │  ┌────────────────┐  │  │  验证规则       │
│ │SELECT│ │  │  │ 性别：○男 ○女 │  │  │  [无          ▼]│
│ └──────┘ │  │  │                │  │  │                 │
│          │  │  └────────────────┘  │  │  SDTM 映射       │
│ ┌──────┐ │  │  ┌────────────────┐  │  │  [DM.DMAGE    ▼]│
│ │RADIO │ │  │  │ 电话：________ │  │  │                 │
│ └──────┘ │  │  │                │  │  │  ─────────────  │
│          │  │  └────────────────┘  │  │                 │
│          │  │                       │  │  [保存] [预览] │
│          │  │  [+添加节]            │  │                 │
│          │  └──────────────────────┘  │                 │
└──────────┴─────────────────────────────┴─────────────────┘
```

**验收标准**：
- [ ] 支持拖拉拽添加/删除/移动字段
- [ ] 字段配置完整（名称、类型、验证规则）
- [ ] CDASH 命名规范提示
- [ ] SDTM 映射配置界面
- [ ] 模板保存和复用
- [ ] 表单预览功能
- [ ] 设计器性能（100+ 字段流畅操作）

#### 5.3.2 数据录入与查询管理（P0）

**需求描述**：
研究者端数据录入和后台查询管理。

**功能点**：

1. **数据录入界面**
   - 按访视（Visit）组织数据
   - 进度保存（自动保存草稿）
   - 电子签名（必填字段签名）
   - 数据锁定（锁库逻辑）

2. **数据验证**
   - 实时验证（范围、格式）
   - 逻辑检查（跨字段、跨表单）
   - 异常值标记

3. **查询管理**
   - 查询发起（自动/手动）
   - 查询回复（研究者端）
   - 查询关闭（数据管理员确认）
   - 查询日志

**验收标准**：
- [ ] 支持在线数据录入
- [ ] 自动保存草稿
- [ ] 验证规则实时生效
- [ ] 查询流程完整（发起 - 回复 - 关闭）
- [ ] 审计追踪完整

#### 5.3.3 SDTM 数据导出（P0）

**需求描述**：
将 EDC 数据导出为符合 SDTM 标准的数据集。

**功能点**：

1. **导出配置**
   - 选择导出域（DM/AE/CM/LB 等）
   - 选择受试者范围
   - 导出格式（CSV/SAS 数据集）

2. **数据转换**
   - 自动应用字段映射
   - 派生变量计算
   - 数据格式标准化（日期、单位）

3. **质量检查**
   - SDTM 标准合规检查
   - 必需字段完整性检查
   - 数据字典生成

**验收标准**：
- [ ] 支持多域数据导出
- [ ] 导出的数据符合 SDTM 结构
- [ ] 支持 CSV 和 SAS 格式
- [ ] 生成数据字典文档
- [ ] 导出日志可追溯

### 5.4 IWRS 模块

#### 5.4.1 随机化管理（P0）

**需求描述**：
支持多种随机化方案，确保随机化过程公正和可追溯。

**功能点**：

1. **随机化方案配置**
   - 简单随机化
   - 区组随机化（固定/随机区组大小）
   - 分层随机化（按中心、性别等分层）
   - 动态随机化（最小化法）

2. **随机化执行**
   - Web 界面随机化
   - 电话随机化（备用）
   - 随机化结果记录
   - 随机化日志

3. **紧急破盲**
   - 在线破盲申请
   - 审批流程
   - 破盲记录（永久记录）

**验收标准**：
- [ ] 支持区组和分层随机化
- [ ] 随机化结果准确
- [ ] 破盲流程完整
- [ ] 随机化日志可追溯

#### 5.4.2 药物供应管理（P1）

**功能点**：
- 药物库存管理
- 药物发运追踪
- 药物回收管理
- 库存预警

**验收标准**：
- [ ] 库存实时更新
- [ ] 发运记录完整
- [ ] 低库存预警

### 5.5 医生病历夹

#### 5.5.1 患者档案管理（P0）

**需求描述**：
医生个人使用的患者数据管理工具。

**功能点**：

1. **患者档案**
   - 患者基本信息（匿名化处理）
   - 档案标签和分类
   - 档案搜索

2. **数据表单自定义**
   - 引用 EDC 模板
   - 自定义表单设计（复用 EDC 设计器）
   - 表单版本管理

3. **数据采集**
   - 在线录入
   - 批量导入（Excel）
   - 数据趋势可视化

**验收标准**：
- [ ] 可创建和管理患者档案
- [ ] 可引用和自定义 EDC 表单
- [ ] 支持数据录入和导入
- [ ] 数据可视化（趋势图）

#### 5.5.2 数据导出与共享（P1）

**功能点**：
- 数据导出（Excel、PDF）
- 报告生成
- 数据共享（加密链接）

### 5.6 系统管理

#### 5.6.1 多租户管理（P0）

**需求描述**：
支持 SaaS 多租户模式，确保数据隔离。

**功能点**：
- 租户创建和管理
- 租户套餐配置（功能、容量限制）
- 租户数据隔离（行级 + 加密）
- 租户账单管理

**验收标准**：
- [ ] 租户数据完全隔离
- [ ] 支持租户级别配置
- [ ] 性能隔离（防止单租户影响全局）

#### 5.6.2 权限管理（P0）

**需求描述**：
细粒度角色权限控制。

**功能点**：
- 角色管理（自定义角色）
- 权限配置（功能权限 + 数据权限）
- 用户 - 角色分配
- 权限继承（项目级别、中心级别）

**验收标准**：
- [ ] 支持自定义角色
- [ ] 权限控制到按钮级别
- [ ] 支持数据范围权限

#### 5.6.3 审计追踪（P0）

**需求描述**：
完整的系统审计追踪，符合 21 CFR Part 11 要求。

**功能点**：
- 所有数据变更记录
- 用户登录和登出记录
- 关键操作记录
- 审计日志查询和导出

**验收标准**：
- [ ] 审计追踪不可篡改
- [ ] 包含操作前后值
- [ ] 支持审计日志导出

---

## 6. 成功指标（Success Metrics）

### 6.1 采用指标（Leading Indicators）

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| eCRF 设计器采用率 | >60% 的项目在 30 天内使用 | 后台统计 |
| 工时填报活跃度 | >80% 的用户每周填报 | 使用频率统计 |
| eTMF 文档上传量 | 平均每项目 >100 份文档 | 文档数量统计 |
| 医生病历夹激活率 | >40% 的注册用户创建档案 | 功能使用统计 |

### 6.2 效果指标（Lagging Indicators）

| 指标 | 目标值 | 测量周期 |
|------|--------|----------|
| 项目上线周期 | <2 周/项目 | 月度评估 |
| 数据录入效率提升 | 相比传统 EDC 提升 30% | 季度评估 |
| SDTM 映射准确率 | >85% 字段自动映射 | 项目结束时评估 |
| 客户满意度（NPS） | >50 | 半年度调研 |
| 年度续约率 | >85% | 年度评估 |

---

## 7. 开放问题（Open Questions）

### 7.1 技术决策（需要技术负责人回答）

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 微服务框架选择（Spring Cloud / Node.js + NestJS / Python + FastAPI） | 技术栈一致性、团队熟悉度 | 高 |
| 数据库选型（PostgreSQL vs Oracle vs 云原生数据库） | 性能、成本、合规性 | 高 |
| 前端框架选型（React vs Vue） | 开发生态、性能 | 中 |
| 文档存储方案（自建对象存储 vs 云服务） | 成本、数据主权 | 中 |

### 7.2 产品决策（需要产品负责人回答）

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 首期支持的治疗领域范围 | 市场需求覆盖 vs 开发成本 | 高 |
| 医生病历夹是否需要独立品牌 | 市场推广策略 | 低 |
| 是否支持本地化部署（On-Premise） | 客户合规需求 vs 维护成本 | 中 |

### 7.3 合规决策（需要合规专家回答）

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 数据跨境存储策略 | 合规风险 | 高 |
| 电子签名符合 21 CFR Part 11 的具体实现 | 法规要求 | 高 |
| GDPR 和《个人信息保护法》的合规措施 | 法律风险 | 高 |

---

## 8. 时间规划（Timeline）

### 8.1 关键里程碑

| 里程碑 | 预计时间 | 交付物 |
|--------|----------|--------|
| M1: 架构设计与评审 | 第 1-2 周 | 技术架构文档、API 设计 |
| M2: 核心数据库上线 | 第 3-6 周 | 数据库 schema、基础数据层 |
| M3: CTMS 基础功能 | 第 7-12 周 | 项目管理、工时、审批 |
| M4: eCRF 设计器 | 第 13-18 周 | 设计器、模板管理 |
| M5: EDC 数据录入 | 第 19-24 周 | 录入界面、查询管理 |
| M6: eTMF 上线 | 第 25-30 周 | 文档管理、完整性监控 |
| M7: IWRS 基础功能 | 第 31-36 周 | 随机化、破盲 |
| M8: 医生病历夹 | 第 37-42 周 | 患者档案、自定义表单 |
| M9: SDTM 导出 | 第 43-48 周 | 数据转换、导出 |
| M10: Beta 发布 | 第 49-52 周 | Beta 版上线、客户测试 |

### 8.2 依赖关系

```
微服务架构 (M1)
    ↓
数据库设计 (M2)
    ↓        ↘
CTMS 核心    EDC 设计器 (M4)
    ↓             ↓
审批流程      EDC 录入 (M5)
                ↓
            SDTM 导出 (M9)
```

---

## 9. 风险评估（Risks）

### 9.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 微服务架构复杂性超出预期 | 中 | 高 | 引入 DevOps 工具、提前架构评审 |
| CDISC 标准理解不足 | 中 | 高 | 聘请 CDISC 顾问、参与 CDISC 培训 |
| 性能瓶颈（大数据量场景） | 中 | 中 | 提前性能测试、数据库优化 |
| 浏览器兼容性（eCRF 设计器） | 低 | 中 | 主流浏览器覆盖、降级方案 |

### 9.2 市场风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 竞争对手快速跟进 | 高 | 中 | 加快产品迭代、建立客户粘性 |
| 客户需求与预期偏差 | 中 | 高 | 定期客户访谈、MVP 快速验证 |
| 合规政策变化 | 低 | 高 | 持续跟踪法规、灵活架构 |

---

## 附录 A：CDISC 标准参考

### A.1 SDTM 主要域列表

| 域代码 | 域名称 | 说明 |
|--------|--------|------|
| DM | Demographics | 人口学数据 |
| AE | Adverse Events | 不良事件 |
| CM | Concomitant Medications | 合并用药 |
| LB | Laboratory Results | 实验室检查 |
| VS | Vital Signs | 生命体征 |
| DS | Disposition | 受试者状态 |
| EX | Exposure | 用药暴露 |
| CM | Clinical Findings | 体格检查 |
| SUPPDM | Supplemental Data | 补充数据 |

### A.2 CDASH 变量命名规范

- 使用大写字母和下划线
- 避免特殊字符
- 遵循标准缩写（如 AGE = Age, WT = Weight）
- 日期变量后缀 DTC（日期时间）或 DT（日期）

---

## 附录 B：术语表

| 术语 | 全称 | 说明 |
|------|------|------|
| CTMS | Clinical Trial Management System | 临床试验管理系统 |
| EDC | Electronic Data Capture | 电子数据采集系统 |
| IWRS | Interactive Web Response System | 交互式网络应答系统 |
| eCRF | Electronic Case Report Form | 电子病例报告表 |
| CDISC | Clinical Data Interchange Standards Consortium | 临床数据交换标准协会 |
| SDTM | Study Data Tabulation Model | 研究数据制表模型 |
| CDASH | Clinical Data Acquisition Standards Harmonization | 临床数据采集标准协调 |
| eTMF | Electronic Trial Master File | 电子试验主文件 |
| GCP | Good Clinical Practice | 药物临床试验质量管理规范 |
| 21 CFR Part 11 | - | FDA 电子记录和电子签名法规 |

---

**文档版本历史**

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| v1.0 | 2026-05-27 | 初始版本 | 产品团队 |
